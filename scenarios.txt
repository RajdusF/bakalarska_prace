#add C:\Users\Filip\Documents\bakalarska_prace\files\exp_refineFinal1_r_cropped.cmap
#add C:\Users\Filip\Documents\bakalarska_prace\files\all_cropped.bnx

#load(added)

f = load(C:\Users\Filip\Documents\bakalarska_prace\files\exp_refineFinal1_r_cropped.cmap)

print(f)

print(f.molecules[2])

ids = []
for i, molecule in enumerate(f.molecules):
    if float(molecule.lnSNRsd) > 0.5:
        print(molecule)
        ids.append(i)
        write_line_based_on_file(f.name, str(molecule))

for i, molecule in enumerate(f.molecules):
    if float(molecule.lnSNRsd) < 0.2:
        print(str(molecule))

print(ids)

f = load(C:\Users\Filip\Documents\bakalarska_prace\files\all_cropped.bnx)

print(f)

for molecule in f.molecules:
    if hasattr(molecule, "AvgIntensity"):
        if float(molecule.AvgIntensity) > 1400:
            print(molecule)
            write_line_based_on_file(f.name, str(molecule))


