# cd C:\Users\Filip\Documents\bakalarska_prace\output
cd /data1/bionano-old/compute/jobs

delete_file("output/correct_molecules.txt")

# filter name *.json size < 500 MB
filter name *.bnx size > 70 MB -MAX:40000

sort by size

select top 20

add *

# loaded_files = pfor(load, added)

# avg_snrs = pfor(average_snr, loaded_files)

# print(avg_snrs)


my_selection = added_files

second_load = []
for x in my_selection:
    second_load.append(load(x))
    
second_snrs = []

for x in second_load:
    second_snrs.append(average_snr(x))
    
