cd /data1/bionano-old/compute/jobs
# cd C:\Users\Filip\Documents\bakalarska_prace\test_files

filter * -d

# filter name *.bnx


# higher = filter name *.bnx size > 1 MB -MAX:200000
# lower = filter name *.bnx size < 50 MB -MAX:200000

# my_selection = higher A lower

# show_files(my_selection)

# add my_selection

# output files_for_testing.txt

# my_selection = input output/files_for_testing.txt

# loaded = []

# for x in my_selection:
#     loaded.append(load(x))
    
    
# for x in loaded:
#     save(x)


# loaded = pfor(load, my_selection)

# pfor(save, loaded)

# avgs = []

# for x in loaded:
    # avgs.append(average_snr(x))

# avg_snrs = pfor(average_snr, loaded)


# exit