cd C:\Users\filip\OneDrive\Documents\bakalarska_prace
# cd /data1/bionano-old/compute/jobs

filter name *.bnx size < 100 MB -MAX:12

sort by size

select top 20

add *

# Paralelni zpracovani


loaded_files = pfor(load, added)
avg_snrs = pfor(average_snr, loaded_files)
print(avg_snrs)




# Sekvencni zpracovani

second_load = []
for x in added_files:
    second_load.append(load(x))
    
second_snrs = []

for x in second_load:
    second_snrs.append(average_snr(x))
    
print(second_snrs)